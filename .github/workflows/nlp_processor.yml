# .github/workflows/nlp_processor.yml
name: NLP Processor (Minerva)

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *' # Ejecutar cada 30 minutos

jobs:
  run_nlp_processor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt
        
      - name: Test componentes sin BD (IA Local)
        run: |
          echo "🧪 Probando componentes del sistema..."
          python test_without_db.py
          
      - name: Run NLP Processor - Fase 1 (Tags)
        env:
          NEON_CONN_STRING: ${{ secrets.NEON_CONN_STRING }}
        run: |
          echo "📋 FASE 1: Extrayendo tags con IA..."
          python main_step1_tags.py
          
      - name: Run NLP Processor - Fase 2 (Clustering)
        env:
          NEON_CONN_STRING: ${{ secrets.NEON_CONN_STRING }}
        run: |
          echo "🔗 FASE 2: Agrupando artículos similares..."
          python main_step2_clustering.py
          
      - name: Run NLP Processor - Fase 3 (Títulos IA)
        env:
          NEON_CONN_STRING: ${{ secrets.NEON_CONN_STRING }}
        run: |
          echo "✨ FASE 3: Generando títulos con IA..."
          python main_step3_titles.py
        
      - name: Resumen
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Procesamiento completado"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🤖 IA 100% local (Hugging Face)"
          echo "💰 $0 costo"
          echo "🚀 Sin rate limits"
          echo "📦 Modelos descargados automáticamente (~1GB, una vez)"