# .github/workflows/nlp_processor.yml
name: NLP Processor (Minerva)

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *' # Ejecutar cada 30 minutos

jobs:
  run_nlp_processor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt
          
      - name: Run Complete NLP Pipeline
        env:
          NEON_CONN_STRING: ${{ secrets.NEON_CONN_STRING }}
        run: |
          echo "🚀 Ejecutando Pipeline Completo de NLP..."
          python run_pipeline.py
        
      - name: Resumen
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Procesamiento completado"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🤖 IA 100% local (Hugging Face)"
          echo "💰 $0 costo"
          echo "🚀 Sin rate limits"
          echo "📦 Modelos descargados automáticamente (~1GB, una vez)"